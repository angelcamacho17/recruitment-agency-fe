export const environment = {
  production: true,
  apiUrl: 'https://mv-whatsapp-backend.onrender.com/api',
  claudeApiKey: '' // NUNCA pongas la API key en producción - usar variables de entorno del servidor
};
